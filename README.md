# Cognifyz WebForm App

A full-stack web app built using Express.js, EJS, and vanilla JS to register users via both traditional and API methods.

## How to Run

1. Install dependencies:
   ```bash
   npm install
